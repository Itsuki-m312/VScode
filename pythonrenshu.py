print("konbanha")

print("aiueo")

3**2
3/2
0.1 + 0.2

#python.jp、変数
(100 * 5) + (40 * 8) + (80 * 5) + (60 * 9) + (90 * 20) + (110 * 10)

momo = 100 * 5
mikan = 40 * 8
nashi = 80 * 5
kiwi = 60 * 9
suika = 90 * 20
kaki = 110 * 10

print(momo + mikan + nashi + kiwi + suika + kaki)
#対話モードになっているとき（>>>がターミナルに表示されていたら）
#ターミナルにexit()を入力して解決-エラー例：File "<stdin>", line 1 
#emailをgithub標準のメアドにする0
print(abs(-123))
minus_value = 100 * -1
print(abs(minus_value))
#absは絶対値を表示する

print(round(1.124 , 2))
#roundは有効桁数を指定して、その桁数にした値を表示するround(桁数を変更したい数字,有効桁数)

print(min(3, 4, 90, 5))
print(max(4, 5, 3, 9, 20))
#minは最小の数、maxは最大の数を戻り値として返す　好きなだけ引数を指定できる

print(int(10.5))
print(int(5 / 2))

print(float(30))
#intはfloat型（小数）の値を整数に変換する（小数点以下を切り捨てる）
#floatは整数の値をfloat型に変換する
#テスト
#テスト２回目